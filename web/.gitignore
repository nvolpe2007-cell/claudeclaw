/node_modules
/.next
/out
next-env.d.ts
*.tsbuildinfo

# Prototype feed data (base64 videos) — don't commit user content
/data/posts.json

# Secrets — never commit real keys
.env
.env.local
.env*.local
